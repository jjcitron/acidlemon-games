# Goals

Standing goals live here as `loop/goals/<name>.md`:

```markdown
predicate: <shell command; exit 0 means still true>
status: active
```

Predicates are commands, not adjectives. Re-verify daily forever. Flaky predicates
get quarantined (with a note in `loop/memory/STATE.md`), not deleted.
