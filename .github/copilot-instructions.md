# GitHub Copilot project instructions

Identify the repository root. Read root `AGENTS.md`, `CLAUDE.md`, `GROK.md`, `GEMINI.md`, and `.github/copilot-instructions.md`, then follow `loop/coordination.md` and every referenced runtime artifact. These instructions are complementary.

Claude is Joel's primary in-repo developer/orchestrator. Preserve every harness's rules and active lanes. More-specific nested instructions govern their subtrees. Never work through another live global or repository-local claim.
